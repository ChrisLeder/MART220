/*class Cube extends threeDShape

constructor(x, y, z, speedX, speedY, speedZ, width, height, depth) {
    super(x, y, z, speedX, speedY, speedZ);
    
    this.width = width;
    this.height = height;
    this.depth = depth;
  }

  draw()
  {
    push();
    super.moveShape();
    texture(img3);
    box(this.width, this.height, this.depth);
    pop();
  }
}*/